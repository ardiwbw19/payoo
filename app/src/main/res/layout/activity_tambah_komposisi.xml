<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="#FFFFFFFF"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.view.komposisi.TambahKomposisiActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <androidx.appcompat.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="60dp"
            android:background="@color/white"
            android:elevation="4dp"
            app:navigationIcon="@drawable/back"
            android:outlineSpotShadowColor="@color/Utama"
            >
            <TextView
                android:id="@+id/toolbar_judul"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:text="Tambah Komposisi"
                android:textColor="@android:color/black"
                android:textSize="20sp"
                android:fontFamily="@font/quicksand_bold" />
        </androidx.appcompat.widget.Toolbar>
        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:background="@color/Kedua"
            android:layout_marginBottom="36dp"
            />
        <EditText
            android:id="@+id/et_nama_komposisi"
            android:layout_width="280dp"
            android:layout_height="50dp"
            android:layout_gravity="center"
            android:background="@drawable/rounded_edit_text"
            android:fontFamily="@font/quicksand_bold"
            android:hint="nama komposisi*"
            android:paddingLeft="30dp"
            android:textSize="12dp"/>
        <EditText
            android:id="@+id/et_harga_komposisi"
            android:layout_width="280dp"
            android:layout_height="50dp"
            android:layout_marginTop="20dp"
            android:layout_gravity="center"
            android:background="@drawable/rounded_edit_text"
            android:fontFamily="@font/quicksand_bold"
            android:hint="harga komposisi*"
            android:paddingLeft="30dp"
            android:textSize="12dp"/>

        <EditText
            android:id="@+id/et_stok_komposisi"
            android:layout_width="280dp"
            android:layout_height="50dp"
            android:layout_gravity="center"
            android:layout_marginTop="20dp"
            android:paddingLeft="30dp"
            android:background="@drawable/rounded_edit_text"
            android:fontFamily="@font/quicksand_bold"
            android:hint="stok komposisi"
            android:textSize="12dp"/>

    </LinearLayout>

    <Button
        android:id="@+id/simpan"
        android:layout_width="280dp"
        android:textSize="14dp"
        android:layout_height="50dp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:background="@drawable/rounded_btn"
        android:fontFamily="@font/quicksand_bold"
        android:gravity="center"
        android:shadowColor="@color/black"
        android:text="Simpan"
        android:textColor="@android:color/white"/>

</RelativeLayout>
